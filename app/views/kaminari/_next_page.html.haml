%li.page-item{ class: current_page.last? ? 'disabled' : '' }
  = link_to t('kaminari.pagination.next.icon'), url,
    rel: 'next',
    remote: remote,
    class: 'page-link',
    title: t('kaminari.pagination.next.text'),
    disabled: current_page.last?
